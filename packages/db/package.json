{
  "name": "@piphacklup/db",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter @piphacklup/core build && tsc -p tsconfig.json",
    "check": "pnpm typecheck",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "lint": "pnpm typecheck",
    "test": "vitest run --passWithNoTests",
    "typecheck": "pnpm --filter @piphacklup/core build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@piphacklup/core": "workspace:*",
    "@neondatabase/serverless": "^1.0.2",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.1",
    "drizzle-kit": "^0.31.8",
    "typescript": "^6.0.3",
    "vitest": "^4.0.15"
  }
}
